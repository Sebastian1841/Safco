version: '3.9'
services:
  backend:
    build: ./Backend
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend
    environment:
      - VUE_APP_BACKEND_URL=http://backend:5000
